# facecook-be — 기여 가이드

정리 기준일: 2026-09-03

이 레포는 face 콕(제52회 용마대동제 소개팅 부스) 서비스의 **백엔드 전용**
레포다. 프론트는 `facecook-fe`(Next.js)로 분리되어 있다.

## 브랜치 전략

- `main`에서 기능별 브랜치를 파생하는 단순 구조 (`main` ← `feat/기능명` 형식).
  별도 통합용 `dev` 브랜치는 두지 않는다
- 새 브랜치는 항상 **최신 `main`**에서 생성한다
- 브랜치명: `{타입}/{작업내용}` — 영문 소문자, 단어는 하이픈(`-`)으로 구분
  - 예: `feat/chat-websocket`, `fix/mission-step-order`, `docs/api-spec-update`
- `main`에 직접 push하지 않는다. 작업이 끝나면 PR을 올려서 머지한다

## 커밋 메시지 컨벤션

형식: `타입: 작업 내용`

| 타입 | 사용 시점 |
| --- | --- |
| `feat` | 새 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 추가/수정 (코드 변경 없음) |
| `ref` | 동작 변화 없는 코드 구조 개선(리팩터링) |
| `chore` | 빌드 설정, 패키지 업데이트, 그 외 잡무성 변경 |

예시:

```
feat: 콕 하루 3회 제한 로직 추가
fix: 신고 처리 시 영구정지 반영 안 되는 버그 수정
docs: API 응답 예시 갱신
ref: 매칭 판단 로직 서비스 계층으로 분리
chore: Flyway 버전 업데이트
```

하나의 커밋에 여러 타입이 섞인 작업은 가능하면 커밋을 나눈다. 안 되면 가장
비중이 큰 타입을 쓴다.

## DB 마이그레이션

- `src/main/resources/db/migration/`에 Flyway 버전 파일로 스키마를 관리한다
- **기존 마이그레이션 파일은 절대 수정하지 않는다.** 스키마 변경은 항상 새
  버전 파일(`V2__xxx.sql`)을 추가한다

## PR 규칙

- `main`에 직접 push 금지, 반드시 PR을 통해서만 머지
- 머지 전 로컬에서 정상 동작(빌드/테스트 통과) 확인
- API·DB 스펙이 바뀌면 `facecook-fe` 쪽에도 공유한다
- 리뷰어 없이 셀프 머지는 지양하되, 리뷰어 확보가 어려우면 최소한 PR 설명에
  변경 내용을 명확히 남긴다

## 이 레포의 범위

REST API, WebSocket(STOMP), DB 접근, 인증/세션, 외부 연동(이메일, 웹 푸시,
S3)을 담당한다. 화면(UI)은 `facecook-fe`의 책임이고 이 레포에 두지 않는다.

## 이 문서 범위 밖

- 이슈 템플릿, 라벨 규칙은 필요해지면 별도로 추가한다
- 커밋 메시지 포맷을 자동 검증하는 도구(husky 등)는 지금은 두지 않는다
